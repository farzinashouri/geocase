"""OpenRouter runner for the bare track (Plan 15 Phase 4, stripped to the
minimum viable one-off: openrouter, bare, extract, sandbox, orchestrator).
The agentic track is run manually per the Phase 5 protocol until expansion."""
