id: polygon_wkb_baseline
title: Polygon WKB Baseline
description: >
  Canonical baseline polygon stored as raw WKB to exercise the custom
  single-geometry binary loader path.
category: vector
format: WKB
test_tier: unit
size_class: tiny
storage_class: bundled
redistributable: true
schema_version: "1.0"
status: validated

tags:
  - vector
  - polygon
  - valid
  - baseline
  - wkb
  - cross_format_canonical

risk_types:
  - format_comparison
  - binary_geometry_parsing

behavioral_goal: >
  Provide a canonical polygon encoded as WKB so plain binary geometry
  loading can be compared against driver-backed vector formats.

expected_capabilities:
  - load
  - geometry-validation
  - bounds-check

loader_hint: geopandas
geometry_type: Polygon
crs: EPSG:4326

files:
  primary: polygon.wkb
  notes: notes.md

source:
  name: geocase-curated
  license: MIT

assertions:
  expect_loadable: true
  expect_valid_geometry: true
  expect_crs: true
  expected_epsg: 4326
  expected_geometry_types:
    - Polygon

params:
  canonical_source_case_id: simple_valid_polygon
