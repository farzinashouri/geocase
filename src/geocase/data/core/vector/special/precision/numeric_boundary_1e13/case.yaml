# Generated by scripts/generate_numeric_boundary_fixtures.py -- do not edit by
# hand. Plan 44 phase 2.
id: numeric_boundary_1e13
title: "Numeric Boundary: 1e-13"
description: >
  A single-point GeoJSON whose coordinate one decade above the affected magnitude class.
  The upper bracket. A brute force over 300000 coordinates puts GDAL's zeroing class at exactly |v| in [1e-14, 1e-13), so this value must survive. A consumer that loses it has a wider bug than the one round 6 found.
category: vector
format: GeoJSON
test_tier: unit
size_class: tiny
storage_class: bundled
redistributable: true
schema_version: "1.0"
status: validated

tags:
  - vector
  - geojson
  - precision
  - numeric_boundary
  - single_variable

risk_types:
  - failure_mode/reference_implementation
  - precision/denormal_magnitude
  - precision/formatter_boundary

behavioral_goal: >
  Establish whether a text write/read cycle returns this coordinate unchanged.
  The expected answer ships with the case, so a consumer is graded rather than
  left to re-derive what "survived" means.

expected_capabilities:
  - load
  - geometry-validation
  - coordinate-precision-check

loader_hint: geopandas
geometry_type: Point
crs: EPSG:4326

# Generated by scripts/catalog_extent.py -- do not edit by hand.
# west > east means the box crosses the antimeridian.
extent:
  west: 0.0
  south: 0.0
  east: 0.0
  north: 0.0

files:
  primary: numeric_boundary_1e13.geojson
  notes: notes.md

source:
  name: geocase-synthetic
  license: MIT

assertions:
  expect_loadable: true
  expect_valid_geometry: true
  expect_crs: true
  expected_epsg: 4326
  expected_geometry_types:
    - Point
  # Plan 44 phase 2.2 -- the answer, typed, so the gate and the consumer read
  # the same field.
  expected_roundtrip_coordinates:
    - [1e-13, 1e-13]

params:
  generator: scripts/generate_numeric_boundary_fixtures.py
