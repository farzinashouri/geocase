id: landcover_small
title: Land Cover Small
description: >-
  A categorical land-cover raster where 0 is NoData, not a class. Resampling or averaging
  categorical values silently invents classes that do not exist.
category: raster
format: GeoTIFF
test_tier: integration
size_class: tiny
storage_class: bundled
redistributable: true
schema_version: "1.0"
status: validated

tags:
  - raster
  - geotiff
  - landcover
  - categorical
  - product:landcover
  - geography:utm
  - delivery:single-file

risk_types:
  - colormap_dropped
  - category_misread

behavioral_goal: >
  Confirm GeoCase preserves a categorical land-cover raster and its colormap.
  Every pixel is classified: the raster declares no NoData, because 0 doubling
  as both a class and a sentinel is the ``ambiguous_zero`` risk and deserves an
  explicit case of its own rather than a silent declaration here (Plan 28).

expected_capabilities:
  - load
  - colormap-check

loader_hint: rasterio
crs: EPSG:32633

files:
  primary: landcover_small.tif

source:
  name: geocase-synthetic
  license: MIT

assertions:
  expect_loadable: true
  expect_crs: true
  expected_epsg: 32633
  expected_band_count: 1
  expected_dtype: uint8
  expected_shape: [16, 16]
  expected_band_names: [landcover]
  expected_compression: deflate
  expected_colormap_present: true

params:
  generator: scripts/generate_raster_fixtures.py
