id: geotiff_multiband_small
title: GeoTIFF Multi-Band Small
description: >
  A small three-band GeoTIFF with explicit NoData pixels in all bands.
  Tests that raster loaders preserve band count and allow reading distinct
  per-band values without collapsing the dataset to a single band.
category: raster
format: GeoTIFF
test_tier: unit
size_class: tiny
storage_class: bundled
redistributable: true
schema_version: "1.0"
status: validated

tags:
  - raster
  - geotiff
  - multiband
  - bands

risk_types:
  - band_loss
  - incorrect_band_order
  - nodata_ignored

behavioral_goal: >
  Confirm that GeoCase can open a small multi-band raster, preserve its
  declared band count, and expose each band as distinct pixel data.

expected_capabilities:
  - load
  - multiband-read
  - nodata-check

loader_hint: rasterio
crs: EPSG:32633

files:
  primary: multiband_sample.tif
  notes: notes.md

source:
  name: geocase-curated
  license: MIT

assertions:
  expect_loadable: true
  expect_crs: true
  expected_epsg: 32633
  expect_nodata: true

params:
  nodata_value: -9999
  band_count: 3
  dtype: float32
