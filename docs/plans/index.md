---
description: Index of GeoCase's active plans — one scoped document per deliverable, with the single roadmap that sequences them and an archive of superseded plans.
---

# Plans

This folder holds GeoCase's forward-looking documents: what is planned, and in what order.
Nothing here describes how the project works today — that lives in
[`docs/contributing/`](../contributing/workflow.md) and
[`docs/reference/`](../reference/codebase-summary.md).

| Plan | Status | What it covers |
|---|---|---|
| [Roadmap](development-plan.md) | **Active — the single roadmap** | Where the project stands, the active sequence, the open user actions blocking it, and a decision log covering every archived plan. |
| [Restart, Spec-First](20-restart-spec-first.md) | **Proposed — overarching** | Acts on three independent external evaluations (one adopter, two rejectors). Splits the project: ship the constants **as scope-guard functions** in a new zero-dependency repo, `geofacts` (formerly `geospatial-spec`); gate the fixture generator on five interviews and rebuild it around a metadata-adversarial primitive with spec-accurate products as presets; demote the benchmark from product to instrument; delete the catalog-as-product surface Plan 14 rejected. Carries a pre-committed 90-day stop condition. |
| [Adoption Action Plan](21-adoption-action-plan.md) | Proposed; Track A subsumed by [Plan 25](25-ship-geocase-as-a-package.md) | Sequences [Plan 20](20-restart-spec-first.md)'s open user actions against the measured release blockers. Release readiness is not the blocker; the funnel around it is. Five tracks, of which A (publish `geofacts`, formerly `geospatial-spec`) is the critical path — geocase declares a runtime dependency that is not yet installable from PyPI. |
| [Portfolio Direction](22-portfolio-direction.md) | **Recorded 2026-08-16 — authorises no building** | Records where all five projects stand after a fourth external evaluation, names the four-gates-four-documents-zero-users pattern, bounds what `geofacts` is actually for, and identifies that `GeoCase_Studies` and `passify` are the content and serving halves of one product with a user who already exists. Sequencing only. |
| [Catalog Site on an Owned Domain](24-catalog-site-on-owned-domain.md) | **Proposed 2026-08-17 — blocked on the domain** | Publishes the catalog site that is already built and never deployed: 188 generated pages with `Dataset` JSON-LD, rendered as Astro on a domain you own. Amends [Website Plan](archive/website-plan.md), reversing its Astro rejection and domain deferral. Carries a pre-committed 90-day Search Console gate. |
| [Ship GeoCase as a Package](25-ship-geocase-as-a-package.md) | **Steps 1–6 and 8 implemented; `geocase 1.0.0rc2` on TestPyPI 2026-08-24, all clean-venv assertions pass — step 7 is the deliberate stop, 9 open** | Executes the upload the library has been ready for since Plan 11. Confirms [Plan 21](21-adoption-action-plan.md)'s track A by measurement: `geofacts` is a hard runtime dependency that resolves only from a sibling checkout, so it must reach PyPI first. Rehearses on TestPyPI as `1.0.0rc2` rather than spending the immutable `1.0.0`. Adds the two non-packaging steps with real adoption leverage: a README that leads with a failure, and validation against a real library's test suite. **Correction on implementation:** step 3 had the MB contradiction backwards — 2.1 MB is the real payload and the plan's 4.2 MB was `du` block-padding across 572 tiny files, so the figures were fixed the other way. Most of steps 2 and 4 were already closed by [Plan 26](26-docs-truth-pass-and-seo-prep.md); the residue plus two phantom GitLab CI claims in `workflow.md` were fixed. Step 8 was taken out of order (it depends on nothing being published): the README now opens on `geotiff_nodata_small`, where two `-9999` pixels in a hundred turn a 48.08 m mean into −152.86 m — example executed, not just written. **Step 5 done 2026-08-24:** `geofacts` reached real PyPI as **`0.1.2`**, not the planned `0.1.1` — the added sdist target is a source change, so shipping it under `0.1.1` would have meant force-moving a pushed tag. Its sdist deliberately carries `scripts/` and `vendored/` as well as `src/`+`tests/`, since the drift gate regenerates the single-file copy and diffs it; verified by running the suite from an unpacked sdist. GeoCase's floor moved to `geofacts>=0.1.2`. **Step 6 done 2026-08-24 — the rehearsal is green and it earned its keep:** `geocase 1.0.0rc2` is on TestPyPI and installs clean, with all four assertions passing (27 names, 135 cases, a case materialized from `site-packages`, and `plugins: geocase-1.0.0rc2` in pytest's header with no `conftest.py`). The first tag **failed** at `twine check` — current hatchling emits `Metadata-Version: 2.5`, which the pinned `twine==6.1.0` rejects; the artifact was never at fault, only the validator, and an unpinned `[build-system] requires = ["hatchling"]` was what let the backend drift past the twine pin. Fixed by `twine==7.0.0` + `hatchling>=1.27,<2`, then the tag was re-cut (safe: the failed run uploaded nothing). This also closed verification 7 — `geofacts` resolved from real PyPI with no sibling checkout on the path — and verification 8, the README's −152.86 / 48.08 example, re-run against the installed package rather than the local conda env. |
| [Docs Truth Pass & SEO Prep](26-docs-truth-pass-and-seo-prep.md) | **Implemented 2026-08-23 — except the `social` plugin** | Closed the gap between what the docs claimed and what the code does: a stale 134-case count that was an executable assertion in `recipe/meta.yaml`, install instructions for packages that do not resolve, and two completed renames (`geospatial-spec` → `geofacts`, `geocase.synth` → `geocase.raster`) never propagated into the case metadata that generates the published pages. Archived nine done/rejected/superseded plans and replaced the stale roadmap. Laid SEO groundwork **host-neutrally** — per [Plan 24](24-catalog-site-on-owned-domain.md) the canonical URL stays undecided — of which the load-bearing item was rendering the 123 cases' `notes.md` prose that no page showed. **Open Graph card generation (`social`) did not land** — it needs the native `libcairo` library, absent from the docs runner. |
| [Close Plan 26 Findings](27-close-plan-26-findings.md) | **Proposed 2026-08-23** | Closes the three things [Plan 26](26-docs-truth-pass-and-seo-prep.md) surfaced but did not predict. The one with leverage: **no case declares an `axis_order` or `crs_mismatch` risk type**, though the README now leads with both and Plan 24 pre-commits to measuring both — so the site would rank for queries the catalog cannot answer. Adds the two missing cases, reports on a risk vocabulary where 75 of 111 terms apply to a single case, gates the two docs defect classes that recurred, and sizes the 114 remaining contributor-voiced descriptions (30 of them silently truncated). |
| [Vector-First: Trust the Corpus, Then Sharpen It](28-validate-geocase.md) | **Phase 1 implemented 2026-08-28; Phase 2.1–2.3 implemented 2026-08-29; Phases 2.4–2.6 implemented 2026-08-30 (Phase 2 complete); Phase 3 implemented 2026-08-30; Phases 4–5 proposed** | Two external validation runs of `1.0.0rc2` reached opposite verdicts — pyogrio/GDAL found 2 real bugs and said "worth shipping"; rio-tiler found 0. Both bugs came from `vector/special/` cases built around a named failure mode, not from the 61 baselines: the corpus works where it is curated around failure modes and is thin where it is curated around format coverage. **Phase 1 (done) fixes the trust problem:** `validate_catalog.py` had never opened a data file, so 6 cases declared nodata with zero nodata pixels and `hole_center_nodata` shipped as the exact *inverse* of its description — NoData on the outer border, interior fully valid — meaning a consumer testing interior-hole preservation got a green light from a case that could not test it. Adds `geocase.catalog.content` + `scripts/validate_case_content.py` to the `catalog` CI job, brings `footprint_edge_cases` under the regeneration gate so the footprint is derived from the raster's own mask, and — with the void restored — makes a real `gdal_footprint` divergence assertable for the first time. Phases 2–3 are the pyogrio asks (driver prerequisites, error taxonomy, `known_divergences`, a differential helper, ~10k-feature cases); Phase 4 records the raster oracle backlog without starting it. **Phase 2.1–2.3 implemented:** this plan's own counts were stale (Plans 32 and 34 took the catalog 135 → 150), so they were re-measured: **113 vector** cases, **20** of which no OGR consumer can open — 13 WKB/WKT and 7 Arrow-family, not the 12+7 recorded here. A probe re-verified Phase 1's correction rather than trusting it: `pyogrio.read_info` fails exactly those 20 while `VectorCase.load()` opens all 113, so `required_drivers` is documented as *consumer* discoverability throughout. §2.1's open decision resolved **for `required_drivers` alone**: `loader_hint` is not a label but a dispatch key in `loaders/generic.py` and an exported `Literal`, so adding a `shapely` member would be a v1.0 schema change to express something `required_drivers` states more precisely — it also separates *why* a case is closed to OGR (no driver exists vs. an installable plugin is missing). New `NO_OGR_DRIVER = ""` sentinel, deliberately falsy so the natural consumer filter excludes bare-blob cases for every possible driver set. **Unplanned:** the catalog page generator rendered the sentinel as an empty pair of backticks — the least useful cell on the pages where it carries the most information — so `_required_drivers_cell` renders it as prose; and `list_cases`'s docstring now warns against the misreading 2.2 invites, since a user who just gained a `loader_hint` filter will reach for it to ask "can my reader open this?", which it cannot answer. Gates green: all ten catalog gates, 1897 tests, 1389 examples, ruff, mypy, mkdocs --strict. **Phase 2.4 implemented:** `ExpectedErrorKind` (`unparseable_geometry` | `unsupported_format` | `missing_driver` | `invalid_crs` | `invalid_topology`) and `AssertionHints.expected_error_kind`, so a harness can assert *how* a case fails rather than only *that* it did — the distinction the pyogrio run had to make by hand for all 20 of its failures. A vocabulary, not exception classes, because the class belongs to the consumer. The corpus's curated-failure surface turned out to be **exactly one case** (`unclosed_ring_polygon` is the only `expect_loadable: false` case in 150), so the field is mostly forward-looking and its test is written as a corpus invariant rather than an id list. **Unplanned:** the field is gated in *both* directions — a model validator rejects it on a case that never fails, since a failure mode nothing can evaluate is the same declared-but-ungated defect Phase 1 exists to close — and `classify_error` (exported, so a consumer's harness uses the gate's own mapping) returns `None` for an unmapped exception rather than snapping to the nearest term. The `expect_loadable` × `expect_valid_geometry` matrix ships in `adding-a-case.md`, documenting the asymmetry the gate leaves silent: `true` is enforced, `false` is not, because `false` means two different things. Gates green: all ten catalog gates, 1909 tests, 1389 examples, ruff, mypy, mkdocs --strict. **Phases 2.5–2.6 implemented, completing Phase 2:** shipped together, since 2.5 is a record nothing reads until 2.6 reads it and 2.6's `known` outcome is untestable without 2.5. `KnownDivergence` + `CaseMetadata.known_divergences` (additive, `[]`) — a *record*, not an assertion, deliberately outside `AssertionHints` because nothing can gate whether a divergence still reproduces: that depends on the reader the user installed. `geocase.differential` reads every case two ways and reports the disagreements, the mode with external evidence behind it (both defects the pyogrio run found came from comparing a consumer against *itself*, so neither path needs to be an oracle). **The plan's demanded end-to-end proof ran, on a different GDAL than the original report:** pyogrio 0.12.1 / GDAL 3.12.2 reproduces the GPKG finding as `row count differs: 2 vs 3`, reported `known` with `consumer=` and `diverged` without — so the seeded `version_range` was widened from the report's single 3.13.3 build to `GDAL 3.12-3.13`. **Unplanned:** the first corpus-wide run reported 7 diverged, all of them the `None`-vs-`nan` KML noise the report had predicted in advance; left in it is 7 findings hiding 1, so `default_compare` treats `None`/`NaN`/`NaT`/`NA` as one missing value and stops there (`""` and `0` are values a reader returned). Four outcomes, not three — `errored` is split from `diverged` because a crash and a wrong answer need different triage, and it is often the most interesting row (the pyogrio `fid_as_index` crash surfaced exactly that way); and both paths failing *identically* is `agree`, since otherwise every `expect_loadable: false` case reports a finding. Gates green: all ten catalog gates, 1937 tests, 1392 examples, ruff, mypy, mkdocs --strict. **Phase 3 implemented, catalog 150 → 153:** three ~10k-feature vector cases under a new `vector/special/scale/`, each with one defect placed *past* a batch boundary — a bowtie at index 9,999, a first NULL after 10,000 non-NULL ints, a UTC offset that changes only at the last row. The size is not the point, discriminating power is: with one feature every batch boundary is the same boundary and every partial read is the full read, so probes for `skip_features`/`max_features`/Arrow chunking executed without being able to fail. All three divergences were **verified to reproduce** on pyogrio 0.12.1 / GDAL 3.12.2 rather than taken from the report. **Two findings the plan did not anticipate.** The cases are `small` and stay in the wheel, not the assumed `medium`-plus-manifest: GPKG of repetitive rows deflates ~6x, so the trio costs ~820 KB of wheel (456 KB → 1.25 MB against an unchanged 2 MB ceiling) — measured before landing, with `verify_dist.py` now recording that headroom is 61% and naming the trigger for moving the *next* trio to a manifest. And the timezone case's divergence exists **only because the file violates GeoPackage requirement 15**: building the conformant all-`Z` version showed no dtype instability whatsoever, so the non-conformance is now a declared property (`gpkg_datetime_conformant: false` + a `spec_nonconformance` risk type) rather than an undeclared cause of the interesting behaviour — the same defect class as a declared assertion nothing checks. The cases also immediately found a real defect in the example corpus: `unary_union` raises `TopologyException` merging an invalid geometry with valid neighbours, which every single-feature invalid case had left unreachable, failing 18 example tests. Gates green: all ten catalog gates, **1966 tests**, **1421 examples**, ruff, mypy, mkdocs --strict, wheel 1.25 MB. |
| [Real Geometry Previews and a Single Compare Page](29-real-geometry-previews-and-compare-page.md) | **Implemented 2026-08-28** | Makes the catalog's diagrams show the catalog's data. `catalog_svg.py` draws from metadata only, so every `Polygon` case renders one hardcoded quadrilateral — `dateline_crossing_polygon` and `simple_valid_polygon` are the same picture, and the schematic hides the edge case each case exists to demonstrate. Verified feasible: **103 of 104 vector cases load real coordinates through `geocase.load_case().load()`**, and the lone failure (`unclosed_ring_polygon`, deliberately malformed) is why the archetype fallback stays. Also verified that the dependency objection is narrower than the docstring claims — the `catalog` CI job already installs `.[raster,vector]` and is the only job running the generator, so **no CI change is needed**. Adds a filterable compare-all table by pointing the existing, already-working `_case_grid()` machinery at the full corpus for the first time. Phase 3 shipped raster pixel previews as separate PNGs under `docs/_generated/catalog/previews/` — 17 cases, gated by `generate_raster_previews.py --check`, written by a hand-rolled `struct`+`zlib` encoder so no image dependency was added. Also fixed Phase 2's compare-page links, which all resolved one directory too deep. |
| [Unpublish Internal Docs and Surface the Catalog](30-unpublish-internal-docs-and-surface-catalog.md) | **Implemented 2026-08-28** | Fixes both directions of the same defect: the published site has no exclusion mechanism at all — `not_in_nav` silences the nav warning but still serves the page — so all 260 markdown files ship, including the whole planning log and the five internal reports in `docs/geocase_validate/` that have no nav entry and are reachable only by URL. Meanwhile the 187 generated catalog pages, the most valuable user-facing content the project has, are buried as items 1–2 of a six-item **Reference** tab. Switches the three internal trees to `exclude_docs` (core mkdocs 1.6, no new plugin, no `[docs]` extra change) and promotes the catalog to a top-level tab. The load-bearing step is the link cleanup: eight published pages link into `plans/`, and `mkdocs build --strict` fails on any relative link to an excluded file. |
| [Case Geography: Extents, Region Labels, and World Maps](31-case-geography-and-world-maps.md) | **Implemented 2026-08-28** | The catalog sells itself on realistic geospatial edge cases but never says **where on Earth a case is**. The only spatial signal published today is a bare CRS string — a coordinate convention, not a location — so nothing on `dateline_crossing_polygon`'s page reveals that it straddles 180°. Adds an optional `extent` (a WGS84 box computed from the real bytes, so it cannot drift from the data) plus an optional hand-written `region` label, surfaces both as a Location row on every case page, and upgrades the thin `spatialCoverage.Place` JSON-LD to a real `GeoShape` box. The visible deliverable is **two** equirectangular world maps on the compare page, vector and raster kept separate — necessary, not cosmetic: 23 rasters share one synthetic UTM 33N transform (~15°E, 40.6°N) and the vector baselines cluster at 10-11°E / 50-51°N, so a single map would pile 130 markers onto two points. Markers therefore cluster with counts. Built as generated inline SVG by extending the dependency-free `catalog_svg.py`, **not** a slippy map: the docs ship zero external JavaScript and no bundler, and keeping the `--check` text-diff gate is worth more than pan-and-zoom. Two constraints found while planning: the antimeridian cases need the `west > east` wrap convention (prior art already exists in the `geojson_bounds` grader) or `total_bounds` reports a world-spanning box, and `case.schema.yaml` is gated by *strict set equality* against `CaseMetadata.model_fields`, so the schema must move in the same change. The `catalog` CI job already installs `.[raster,vector]`, so **no CI change is needed**. |
| [Footprint Ground Truth, and the Missing `ambiguous_zero` Case](32-footprint-truth-and-ambiguous-zero.md) | **Implemented 2026-08-28** | Closes two loose ends from [Plan 28](28-validate-geocase.md) Phase 1, both the same defect: the corpus asserts against the wrong reference. Three of the five `footprint_edge_cases` "expected footprints" are recordings of `gdal_footprint`'s own hull, not the raster's valid-pixel mask — inflated **1.98x / 1.88x / 2.80x**, and `rotated_two_islands` collapses a genuine **MultiPolygon** of two disjoint islands into one polygon spanning the gap. So `examples/test_gdal_footprint.py` compares GDAL against itself at `1e-10`, and the `min_rect_ratio` thresholds (0.74/0.93/0.76) were fitted to the hull — the real shapes score 0.375/0.500/0.275, so `thin_corridor_shape` is validated against a footprint 2.8x too big and cannot fail for the reason it exists. Plan 28's gate misses this because `_check_footprint` compares only *hole counts*, which agree at zero. Splits each fixture into `_truth` (mask-derived, under the regeneration gate) and `_gdal_hull` (recorded behaviour), strengthens the gate to check part count and area, and rewrites the example to assert the divergence. Phase 2 adds the `ambiguous_zero` case, whose only occurrence in the corpus today is the `landcover_small` comment pointing at a case that was never written. **Implemented:** gate observed red on exactly those three cases then green; `nonsquare_diagonal_sparse`'s truth turned out to be **8** disjoint parts, not 2, and `hole_center_nodata` was a fourth hull-fitted `min_rect_ratio` (0.98 vs a real 0.889). New case `landcover_ambiguous_zero_small` (catalog now 136); `ambiguous_zero` registered in [Plan 27](27-close-plan-26-findings.md) §1.3, whose enforcing check stays owed there. |
| [Map Identity, Real Geography, and Non-Trivial Geometry](33-map-identity-geography-and-geometry.md) | **Implemented 2026-08-29** | A review of the world maps [Plan 31](31-case-geography-and-world-maps.md) shipped found the maps working correctly and, in doing so, exposed what the corpus behind them actually looks like. Three defects, all measured. **(1) The map has no identity**: `catalog_svg.py` draws extent rectangles with no `<title>` and no case id, so a reader who spots a footprint cannot learn what it is — only cluster markers carry a tooltip. **(2) 54% of the catalog is in one place**: 58 of 127 placed cases fall inside a single 1° box at 10°E/50°N, because 60 vector cases are format-transcodings of just six hand-authored GeoJSON canonicals that all sit in Thuringia and Copenhagen. **(3) Geometry is trivial and CRS coverage is a single UTM zone**: the maximum vertex count in the entire catalog is **10**, twelve canonicals are plain 5-vertex rectangles, the maximum feature count is 3, and only four CRSs exist (4326 ×105, 32633 ×24, 3857, 3995) — so `geotiff_utm_boundary` and `utm_zone_33_polygon` both resolve to 33N and cross-zone reprojection is untested by the bundled data. Phase 1 wraps each extent in a `<g data-case-id>` with a tooltip and wires hover/click to the compare table through the existing vanilla `catalog-compare.js` (no library, no CDN, still progressive enhancement), and marks the polar caps — whose 180°-wide bbox is honest arithmetic rendered as a lie — with a derived `_is_pole_cap()` predicate rather than a new schema field, since `case.schema.yaml` is gated by strict set equality. Phase 2 adds procedurally generated geometry: a 1537-coordinate Koch ring and a 4096-vertex parametric ring, pure `math` with no PRNG so regeneration stays byte-identical, plus the thumbnail decimation that keeps `compare.md` text-diffable. The single-feature invariant at `generate_vector_fixtures.py:74` is **kept** — it scopes the transcoding families, not the catalog — but its wording is rescoped, since it currently claims more than it means. Phase 3 relocates five of the six canonicals to distinct world regions (adding cases alone would leave the 58-case clump exactly where it is) and adds four UTM cases including a southern-hemisphere zone and a zone-boundary straddle; `simple_valid_polygon` deliberately stays at 10°E/50°N as the documented example. Blast radius of Phase 3 is wide but fully mechanical: ~60 fixture binaries, ~66 extents, every catalog page and both coverage matrices regenerate. **Implemented:** all three phases. The worst 1° box now holds **15 of 139** placed cases (was 58 of 127), the catalog spans **4 UTM zones** (was 1), and the maximum polygon vertex count is **4097** (was 10); catalog 136 → 143 cases. Four things the plan could not see from outside the code: `_decimate` had to live in `catalog_geometry.py` to avoid an import cycle; a `_write_geojson` backend had to be written, since nothing had ever *written* the format the canonicals are authored in; the GPKG dense ring became its own procedural case rather than a transcoding, having no `canonical_source_case_id`; and the 4096-vertex GeoJSON came to **315 KB** at `indent=2` against a 256 KB budget — 91 KB once compacted, caught by the budget test rather than trusted to the estimate. Relocating the canonicals also stranded ~60 transcodings' hand-written `region:` fields (synced) and broke 13 example nodes: twelve are the naive-WGS84 limitation already recorded for `epsg32633`, now reached by three more zones and added as strict xfails; the thirteenth was real, since `_utm_polygon_case()` assumed every WGS84 `utm` polygon has one correct zone — `utm_zone_33n_to_32n_pair` has none by design, so those selectors now skip `utm_zone_boundary`. |
| [Close the Reviewed Catalog Gaps](34-close-reviewed-catalog-gaps.md) | **Implemented 2026-08-29** | Acts on an external geospatial expert's review of the 143-case catalog, which named 16 candidate gaps. Verified against the code: **3 were already covered** (anisotropic pixels — `nonsquare_diagonal_sparse` really is 60 m × 30 m; kissing rings; missing CRS), **1 was read backwards** (`fractal_coastline_polygon` states it has *no* collinear vertices, deliberately), and 12 are real but not equal in value. Half are cut with reasons: alpha-as-nodata would extend `NodataConvention`, a v1.0 `Literal` break, for one fixture; overlapping MultiPolygons sit on the same validity axis as cases already shipped; mixed-timezone datetimes are owned by [Plan 28](28-validate-geocase.md) Phase 3 and need ~10k features to discriminate, so a two-feature version would pre-empt that design. The largest finding is one the review only gestured at: **`latlon_sample.nc` is the only fixture in the repository that cannot be regenerated** — unseeded random values, a single "Analyse structure" commit, no seed recovered across 400 combinations — so every NetCDF gap is blocked behind building the generator that has never existed, which in turn needs the `catalog` CI job to install the `netcdf` extra it currently omits. The second is a defect the review surfaced indirectly and the fix is *subtraction*: `latlon_small` declares `coordinate_order` and `dimension_mismatch` over conventional `(latitude, longitude)` data, so the risk is labeled, not exercised — the same class [Plan 28](28-validate-geocase.md) found six times and [Plan 32](32-footprint-truth-and-ambiguous-zero.md) found in the footprint truths. Four phases: the NetCDF generator and CI extra first (it unblocks the rest and is the only phase touching `ci.yml`), then a bottom-up positive-`e` affine and an `AREA_OR_POINT` differential pair (all 32 rasters use `from_origin`, so a consumer assuming north-up passes the entire catalog), then the packed and CF-time NetCDF cases, then `POLYGON Z` plus the `axis_order` declaration [Plan 27](27-close-plan-26-findings.md) §1.3 already owed — the GML baselines genuinely carry authority-order coordinates on disk and no `case.yaml` says so. Two new `AssertionHints` fields, both additive and `None`-defaulted; `params` entries only ever land with a matching `content.py` check, since `params` is unvalidated. Catalog 143 → 149. **One non-additive item, decided by the user:** `latlon_small` is `status: validated` and ships in the package, and Phase 1.4 replaces its bytes rather than adding a case beside it — declared assertions are all preserved, but the change must reach the changelog or it is indistinguishable from corruption **Implemented:** all five phases; catalog 143 → **150**, one more than the planned 149 because the pixel-anchor pair needed an explicit `Area` control to be a true differential. **Corrections on implementation:** four of the plan's own claims were wrong and are recorded in its header. The plan located `AssertionHints` and the case tree at paths that do not exist. `expected_scale_factor` was described as an existing check to reuse; it had **no consumer at all** — declared in the model, the schema and three raster `case.yaml`s and read by nothing — so Phase 2 gated it on both sides, closing a declared-but-ungated hint of exactly the class [Plan 27](27-close-plan-26-findings.md) §1.2 forbids. The `axis_order` row the plan said it would *flip* in Plan 27 §1.3 did not exist; §4.2 **added** it, and closed Plan 27 §1.1's `axis_order_swapped_pair` instead (`crs_mismatch_overlay_pair` stays owed). The int64 rider would have rewritten ~60 baselines had `_ID_FIELD` — a field *name* — been edited as written; a `VectorSpec.id_value` carries it. **Two findings the plan did not anticipate.** `expect_crs` was to be checked leniently on netcdf so Phase 1 would not go red; implemented strictly it immediately caught `latlon_small` declaring a CRS with no `grid_mapping` and no `crs` variable — so the subtraction grew to include it, and the check can fail. And `_COUNT_CLAIMS` gated 7 of 11 statements of the case count: four phrasings matched no pattern and would have drifted silently through all three renumberings, so the gate was widened to 12 and verified by mutation. Every gate green under conda: 1873 passed / 37 skipped, content validation across all 150 cases, and the five-step break/restore proof — each mutation named its own field and each restore was byte-identical. |
| [Compare Page: Map Interaction, Per-File Downloads, and the Browse Link](35-compare-page-map-interaction-and-downloads.md) | **Implemented 2026-08-30** | Four scoped fixes to the compare page [Plan 29](29-real-geometry-previews-and-compare-page.md) built and [Plans 31](31-case-geography-and-world-maps.md) and [33](33-map-identity-geography-and-geometry.md) mapped, found by using the shipped page rather than by review. **(1) Every hover fires two tooltips**: `world_map()` emits an SVG `<title>` on each hoverable `<g>` as the no-JS fallback, and Plan 33 added a styled HTML tooltip whose stated justification is that the native one "waits about a second, cannot wrap, and truncates exactly the long cluster lists that most need reading" — but the native one still fires on top of it after its delay. Suppressed in JS, not at the generator, transferring the text to `aria-label` so the no-JS fallback and the screen-reader announcement both survive. **(2) A single-case marker does not filter**: the script early-returns on `ids.length > 1` because a one-case marker "has nothing to filter down to", which in use reads as a broken marker — the reader who just filtered by clicking a cluster clicks its neighbour and nothing happens. `apply()` is already set-based, so the fix is deleting the guard. **(3) Case pages name their data files but do not link them**, and the vector tree is nested by geometry type, so a reader cannot guess the path; the Files section becomes GitHub raw links built from the existing `roots.package_root()`. **Bundled zips are an explicit non-goal** — ~7.4 MB of byte-identical generated binaries plus a CI gate to solve what `git clone` solves, and the same problem as the v1.1 remote transport. **(4)** The home page's primary catalog link retargets to the compare page. **Implemented:** all four phases; 153 case pages regenerated with linked Files sections, `--check` green. **Two deviations.** §2.3 asked whether the markers inherited a `:focus-visible` outline — `catalog.css` had **no focus rule at all**, so the ring is new, and it is painted on the marker's `circle` rather than as an `outline`, which SVG honours inconsistently. And the plan's verification line ran `ruff` over `scripts/`, which is outside the lint scope CLAUDE.md defines and carries 37 pre-existing errors; the committed gate (`src tests`) is green and the touched script is `ruff format`-clean. **Amended 2026-08-30 (Phase 5), by the user's decision after using the page.** Phase 2 as shipped kept [Plan 33](33-map-identity-geography-and-geometry.md)'s split — markers press, footprints only hover — and that split was the real complaint: the footprint is the largest target on the map and the one a reader aims at first, so a polygon that does nothing under the cursor reads as a broken map. Plan 33's objection was to a footprint that *navigated away*, not one that filters, and a polygon and the marker over it denote the same case; both now bind through one `bindTarget()`, and Plan 33's bullets are struck through in place. **The second defect is why Phase 2 looked like it had not worked at all:** `world_map()` placed each cluster at its raw centroid, so cases sitting *exactly* on the boundary — the poles at `y=0`/`y=MAP_HEIGHT`, the dateline cases at `x=0`/`x=MAP_WIDTH` — rendered half outside the viewBox with only the inner half as a hit target. `antimeridian_crossing_line`, the marker this plan's own Verification step told the reader to click, was therefore the least clickable thing on the map. Centres are clamped into `[radius, MAP_* - radius]` and the count label follows; the new gate parses every circle out of the full-catalog SVG and caught **six** clipped markers, not the four found by hand. All 213 pages regenerate. A third defect, §5.3: Phase 1 stripped the `<title>` from every footprint and marker but not from the `<svg>` root, and a native `<title>` covers its whole subtree — so the root's became the tooltip for every hover on the map, unstyled and timed to surface over the styled one. Phase 1's gate asserted only that `aria-label` and `removeChild` appeared somewhere in the script, so it could not see this; the new gate pins the selector. |
| [The `1.0.0rc3` Release Runbook and the `crs_mismatch` Gap](36-rc3-release-runbook-and-crs-mismatch.md) | **Phase 1.1 done, Phase 2 implemented 2026-08-30** | Two deliverables sharing a deadline. **Phase 1 is a runbook, not a build:** the executable steps from a dirty working tree to `geocase 1.0.0rc3` on TestPyPI — commit, pull request, the CI watch that confirms the 3.11 floor local conda/3.14 runs cannot, merge, tag from `main`, and approve the `testpypi` environment. The version bump lands on `validation` **deliberately**, so the pull request carries it and CI verifies the artifact against the same `pyproject.toml` a reviewer is reading; `verify_dist.py` parses `project.version` rather than `geocase.__version__`, which would report whatever is installed. **Why another rc rather than `1.0.0`:** [Plan 28](28-validate-geocase.md) Phase 3 took the payload 2.1 → 5.1 MB and the wheel 456 KB → 1.25 MB since the rc2 rehearsal, and PyPI is immutable. Measured green at rc3 in this tree — 1972 passed, all catalog gates, `mkdocs --strict`, and `verify_dist.py` at wheel 1251 KB / sdist 1003 KB with 153/153 cases — so the size risk is now retired rather than assumed. Records the browser steps that cannot be scripted (pending publisher, the `pypi` environment, enabling Pages) and that `gh` is absent though Homebrew 5.1.0 is present. **Phase 2 closes `crs_mismatch`**, verified declared by zero cases while sitting in both the README's opening prose and [Plan 24](24-catalog-site-on-owned-domain.md)'s pre-committed search vocabulary — ranking for a query the catalog cannot answer spends a first impression on a miss. [Plan 27](27-close-plan-26-findings.md) §1.1's `crs_mismatch_overlay_pair` design stands; Plan 34 §4.2 closed only the `axis_order` half and left this owed. No existing case can express it because **a mismatch is a relationship between two inputs** and every candidate is single-layer. Settles Plan 27's open structural question against the model — `files.sidecars` is `list[str]` with a default, so one case with a sidecar needs no model change, and it is preferred because a relationship split across two selectable cases can be selected apart. Notes the count gate has grown from Plan 27's budgeted seven claims to **twelve patterns across seven files**. Explicitly out of scope: Plan 28 Phase 4 (entry condition unmet — the only raster signal declined), real PyPI, and conda-forge. **Phase 2 implemented 2026-08-30:** `crs_mismatch_overlay_pair` ships and the catalog is **154 cases**. One footprint in southeastern Norway written twice — a WGS84 primary and a sidecar holding UTM 33N metres while declaring EPSG:4326 — both individually well-formed and parsing without warning, so the defect exists only in the pair. Reprojecting by the true EPSG agrees to **0.0004 m**; trusting the declaration lands **3359 km** away and returns an empty overlay, which reads as "no features intersect" rather than as an error. Tests written first and watched fail 7/7 on a missing registry key. **Two deviations:** the count gate fired in seven *files* rather than the twelve *patterns* the plan quoted (the patterns collapse per file), and `generate_vector_fixtures.py` never applied — it builds only cases declaring `params.canonical_source_case_id`, so this one is hand-authored and checksum-gated like `utm_zone_33n_to_32n_pair` beside it. The content check was verified to bite by rewriting the sidecar honestly. |
| [The Raster Corpus Earns Its Keep: Two rio-tiler Defects, and the Differential Adapter They Unblock](37-raster-signal-and-differential-adapters.md) | **Phases 1-3 implemented 2026-09-01; Phase 4 not started** | An external differential run against pyogrio, rio-tiler, geocube and fiona found **two real defects in rio-tiler 9.4.3**, and **both came from the raster corpus** — the signal [Plan 28](28-validate-geocase.md) Phase 4 was waiting for and [Plan 36](36-rc3-release-runbook-and-crs-mismatch.md) recorded as declined. **(1)** `Reader.read()`, `.part()` and `.preview()` silently mis-georeference a rotated affine: `read()` returns the raw rotated array while `ImageData.bounds`/`.transform` describe a north-up grid (against a `WarpedVRT` reference of 7 valid pixels — `read()` 9, `part()` 4, `preview()` 9), because `io/rasterio.py:100` wraps in a `WarpedVRT` only `if self.dataset.gcps[0]` and `models.py:606` builds the transform unconditionally from `from_bounds(...)`. **(2)** A bottom-up positive-`e` affine yields an inverted `Reader.bounds` (bottom > top, so `bounds[3]-bounds[1]` is negative) and `feature()` raises `WindowError` where `part()` over the identical area succeeds. Each defect was found by the **only** case in the corpus carrying its convention — `rotated_two_islands` and `bottom_up_dem_small`, the latter added by [Plan 34](34-close-reviewed-catalog-gaps.md) §2 with the explicit rationale that *"all 32 rasters use `from_origin`, so a consumer assuming north-up passes the entire catalog"*, now confirmed against a real consumer on the first run. Two of 34 rasters carry a non-`from_origin` affine; both found a defect and the other 32 found nothing, so the axis that pays is **convention divergence**, currently sampled twice. Overturns Plan 28's *"does not yet earn its keep on the raster side"* — same library, same version, different method: conformance-against-declared-truth found 0, a differential over transform conventions found 2. Phase 1 records both in `known_divergences` and gates the two affines against a regeneration that would quietly normalise them; Phase 2 builds the raster adapter protocol Plan 28 Phase 4 specified but never started, carrying the three lessons the hand-written harness learned the hard way (NaN-vs-NaN must compare equal — two of three initial "findings" were only that; masks are a finding separate from values; shape short-circuits); Phase 3 widens the paying axis by three cases (154 → 157) and explicitly adds no more `from_origin` baselines. **pyogrio is not clean, and this run missed both of its defects** — the two [Plan 28](28-validate-geocase.md) already records, re-verified live on 0.12.1 *and* the latest 0.13.0/GDAL 3.12.4, so neither was fixed upstream. The `fid_as_index=True`+`use_arrow=True` crash reproduces on **GeoJSON only** (GPKG, Shapefile, FlatGeobuf and SQLite pass) and this run probed a GPKG; the GPKG spatial-filter divergence needs `bbox=`/`mask=`, which this run's differential never passed — even though `empty_geometry_gpkg` already carries a `known_divergences` entry naming the exact environment it was running in. Both misses trace to one cause: the run hand-rolled a comparison instead of calling `compare_cases()`, and varied only library-vs-library on a plain read, while both pyogrio defects live in **option space**. That is the plan's sharpest input — a corpus finding needs a case **and** the option combination that makes it discriminate, so Phase 2 must ship documented consumer option-pairs, not just files and an array comparator. geocube read clean over 89 cases × 3 rasterization routes but is **not** clean — a follow-up review pass without the corpus found `rasterize_points_griddata`'s default `method="nearest"` silently ignores `fill`. That pass found **four** issues across the four libraries with **zero overlap** against the corpus's three (rio-tiler 2+1, fiona 1+2, geocube 0+1, pyogrio 0+0), which the plan records because it bounds what a corpus of test *data* can reach: fiona's two `__eq__` defects have no file anywhere in their reproduction, and one review finding — `ImageData.mask` breaking its documented uint8 0/255 contract at every other dtype — was in front of the harness and hidden by its own `mask > 0` comparison, which is why Phase 2.2 now requires masks be compared by equality rather than truthiness. Also records a low-severity fiona gap: `KML`/`LIBKML` are commented out of `drvsupport.py`, so the refusal is deliberate and the defect is an error message that blames GDAL for fiona's own allow-list. |
| [Six Consumers, Eighteen Defects, and the STAC Adapter the Corpus Had to Grow](38-six-consumer-round-2-and-the-stac-adapter.md) | **Phases 1-4 implemented 2026-09-01; Phase 5 not started** | A second external differential run — **titiler, stackstac, odc-stac, lonboard, geoarrow-python, pyproj** — found **18 defects across 7 libraries** — **14 caught by geocase and missed by a pure-review pass**, of which **eleven were found by a specific case** and three by the run rather than by any file — which settles the entry condition [Plan 28](28-validate-geocase.md) Phase 4 has waited on since August: two independent runs have now produced raster defects, so the adapter protocol is justified and this plan starts it. **The instrument was stackstac against odc-stac** — two independent implementations of one operation, so neither is the oracle — with `rasterio`+`WarpedVRT` at the same geobox breaking ties, over 34 rasters × 10 option combinations plus a *pinned* family where both libraries and the reference share one target geobox, making a pixel disagreement a defect rather than a convention. It produced: **odc-stac returning a 1×1 array** for `load(crs="EPSG:4326")` on a 10 m UTM raster because `output_geobox` pairs the caller's CRS with the *native* resolution number without converting units (`_mdtools.py:1166-1181`) — **31 of 34 rasters collapsed to one pixel**, while stackstac derived correct grids throughout; **odc-stac silently dropping `raster:bands` scale/offset** (`_extract_data_values` reads only nodata/data_type/unit), giving values **10 000× from stackstac's** on `ndvi_scaled_int16_small`; and **stackstac rejecting every pystac Item**, because it reads only `proj:epsg` while pystac ≥1.13 migrates that to `proj:code`, breaking its own documented `item_collection()` workflow. **titiler republishes both of [Plan 37](37-raster-signal-and-differential-adapters.md)'s rio-tiler defects over HTTP** — re-verified still live at 9.4.3 before the claim was made: `rotated_two_islands` is served as raw unwarped pixels under north-up `/cog/info` bounds at HTTP 200, and `bottom_up_dem_small` yields `miny > maxy` bounds whose *normalised* `/cog/bbox` request then **500s** — plus four defects titiler adds itself, of which the sharpest is that **`/preview.npy` and `.tif` return the dataset's colormap applied rather than class codes**, so a palettised raster's values are unrecoverable through the API that exists to return values. **Three corrections to the corpus's picture of itself:** two predicted "gaps" already exist — three scale/offset rasters (one of which found the odc-stac defect) and seven nodata-that-is-also-valid rasters from [Plan 32](32-footprint-truth-and-ambiguous-zero.md) — while the CRS monoculture (**31 of 34 EPSG:32633**), the missing multi-item group, and the single rotated raster are real and are Phase 4. **Zero overlap between methods for the third time**, now on 18 findings across 7 libraries: a review pass that opened no case file found four more, three of them *structurally unreachable* by any corpus of test data (no file appears in their reproduction), which lets the rule be stated — a corpus finding needs a byte pattern the consumer cannot represent; a review finding needs a stated promise it breaks. **The run's own failure is recorded too:** the lonboard sweep first reported five divergences that were the harness's fault, because lonboard emits `OGC:CRS84` and the comparator read a correct reprojection as a coordinate error — five false findings against two true ones, which is Phase 3.2's justification for geocase shipping the *comparison* and not only the files. Phase 1 records nine case/consumer divergences and gates the conventions that paid (deliberately recording **none** against the three findings no case found — both stackstac defects and odc-stac's `crs=` one — since a `known_divergences` entry on an arbitrary case would be a false claim about which case found what); Phase 2 ships `geocase.stac` — the Item adapter the run built by hand, emitting both `proj:epsg` and `proj:code` and a **normalised** `proj:bbox`, since rio-stac's is inverted for bottom-up sources — with the lazy size cap and per-load timeout a sweep needs after one was OOM-killed at 3 GB deriving a 3.17×10¹² pixel grid; Phase 3 ships the option-pair matrix and a CRS-aware equality predicate; Phase 4 adds four cases and explicitly adds no more `from_origin` baselines; **Phase 5 files three of the seventeen drafts and flags the distribution question for [Plan 25](25-ship-geocase-as-a-package.md)**. The context section carries the empirical answer to *"but you could write that file in twenty lines"*: a probe of each consumer's **own** test suite at the tested commits finds **zero tests mentioning rotation or a bottom-up affine in any of the five** — against live controls (odc-stac touches nodata in 15 files, lonboard has 22 files with `def test`) — so the difficulty was never writing the file but knowing which one to write, and the review pass that had all six codebases open found **0 of the 14**. Which sharpens the largest open risk: three runs have established that the corpus finds real defects and **zero have established that anyone outside this repository wants them**, which is why filing three reports is a phase rather than an afterthought. |
| [Going Public, Upstream First: The Release, the Site, and the Order the Broadcast Has to Happen In](39-going-public-upstream-first.md) | **Proposed 2026-08-31; Phase 0 done (0.1 + 0.2)** | Six outward-facing actions proposed together — cut a version, publish the docs site, contribute upstream, tell the user's manager, write an article, post on LinkedIn — of which **five are broadcast and one is substance**. This plan sequences them, and its central claim is that the order matters more than any item: **the upstream filings move to the front, not the end**, because they are the only action producing *external* evidence and every other item is materially the same claim (*this corpus finds real bugs in software people depend on*) addressed to a different audience. Today that claim rests entirely on the author's analysis of the author's own corpus; one accepted issue converts it into a claim a third party has agreed with, in public, with a URL — and if two of three come back known, intentional or wrong, that is better learned from a GitHub thread than from a comment under a LinkedIn post. **Corrects the premise it was given.** The proposal assumed [Plans 37](37-raster-signal-and-differential-adapters.md) and [38](38-six-consumer-round-2-and-the-stac-adapter.md) were implemented: **both are `proposed`** and nothing from either has landed. `1.0.0rc3` is **already on real PyPI** (rc1–rc3, all tagged), so "publish a new version" means `1.0.0`, not a first release. `https://farzinashouri.github.io/geocase` **404'd** — `pages.yml` was committed and correct and `mkdocs.yml` named the `site_url`; Pages had simply never been enabled in repository settings. **§0.1 is done:** the site serves, and the plan's assumption that this was a browser-only step was wrong — the REST API enables it, though creation defaults to `build_type: legacy` (serving the repo root) and needs a second call to switch to `workflow`, so the build type must be *verified* rather than assumed. And **nothing has been filed upstream**: 17 ready-to-paste drafts sit unfiled. Phase 0 enables Pages and settles a problem nobody had noticed — the article recommended below *is* the unified validation report, which was **untracked** (the other five files in `docs/geocase_validate/` are in git; this one existed only in the working tree) *and* reachable at no URL, since `exclude_docs` removes `geocase_validate/` from the build **entirely** rather than merely from the nav. **§0.2 is done:** the report is now [`docs/validation.md`](../validation.md), tracked and in the nav between *Philosophy* and *Catalog* as the argument for why the catalog exists, while `geocase_validate/` stays excluded — publishing the 17 unfiled accusations against named libraries is precisely what that exclusion exists to prevent. The move also rewrote the report's closing section, which listed two machine-local paths meaningless to a public reader and linked a plan doc by a relative path that would fail `--strict`, and left one debt for Phase 4: the line *"Nothing has been filed upstream. These are drafts."* is accurate today and false the moment Phase 1 runs. Phase 1 files the three drafts [Plan 38](38-six-consumer-round-2-and-the-stac-adapter.md) §5.1 already selects — **as a person, with no geocase link in the body**, because a corpus link in a bug tracker is the fastest way to have a good report dismissed, and the maintainer's "how did you find this?" is a far better moment to answer — then **waits**, with the entry condition for broadcast set at *filed plus two weeks*, not *accepted*. Phase 2 lands only Plans 37 and 38's **Phase 1** during the wait: the divergence records and the convention gates, which is the one genuine bug-risk item on the list, since both conventions that produced the severest findings of both rounds are carried by exactly **one case each** and a regeneration that quietly normalised either would delete the corpus's only coverage of its most productive axis — after that axis had been publicly claimed as the central result. Phase 3 cuts **`1.0.0`, not rc4**: the size risk rc3 was spent on is measured retired, neither pinned compatibility surface has moved, three external runs have exercised the corpus harder than a fourth rc would, and an rc needs `--pre` at exactly the moment a reader's interest peaks. It settles the one immutable question deliberately — `geocase.stac` and `compare_arrays` are **`1.1`**, safe because `differential` is already a non-`__all__` submodule import so both are additive, and holding `1.0.0` a month for internal completeness is the wrong trade when the largest open risk is external interest. Phase 4 broadcasts in a fixed order with the **manager email ahead of the public posts** — an IP or publishing-policy question is far cheaper surfaced privately by the author than raised by someone else after the post — and carries two editorial corrections to the report: its title (*"What ten geospatial libraries got wrong"*) is more adversarial than its content, which explicitly notes *"neither library wins"*, and **the method claim is the stronger and less inflammatory headline**; and its closing line, *"Nothing has been filed upstream. These are drafts."*, becomes false the moment Phase 1 runs. Venue ranking: own site → Hacker News, then the [Cloud-Native Geospatial Forum](https://cloudnativegeo.org/) blog as the correct audience for STAC/COG tooling; skip Medium. Phase 5 refuses to let the broadcast substitute for adoption, recording [Plan 38](38-six-consumer-round-2-and-the-stac-adapter.md) §5.3's orphaned input — an article produces readers, a **`pytest` fixture pack in a consumer's CI produces a user**, and the accepted-issue thread is the strongest possible moment to offer one — and hands the build decision to [Plan 21](21-adoption-action-plan.md)/[Plan 25](25-ship-geocase-as-a-package.md) rather than settling it here. |
| [Archived plans](archive/index.md) | Superseded | Plans 01–19 and the pre-v1.0 sequencing documents, retained as an implementation log. |

## Rules for this folder

- **One roadmap.** Only [`development-plan.md`](development-plan.md) says "what's next".
  Everything else here is a scoped plan for a single deliverable. Four competing
  "what's next" documents is what forced the July 2026 collapse.
- **Supersede, don't delete.** When a plan is finished or replaced, move it to
  [`archive/`](archive/index.md) with a status banner, and record the outcome in the
  roadmap's Decision log.
- **Plans are not published.** The whole `plans/` tree is excluded from the documentation
  site via `exclude_docs` in `mkdocs.yml` and is read here on GitHub. A published page must
  not link to a plan with a relative path — `mkdocs build --strict` fails on it. Link to the
  file's GitHub URL instead. See
  [Plan 30](30-unpublish-internal-docs-and-surface-catalog.md).
