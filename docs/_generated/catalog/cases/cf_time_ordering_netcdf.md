---
title: "CF Time Units and Non-Conventional Dimension Order"
description: "A CF time axis encoded as \"hours since 2020-01-01 00:00:00\" on a variable whose dimensions run (longitude, latitude, time) -- x before y, time last. Undec…"
---

<!-- Generated by scripts/generate_catalog_pages.py. Do not edit by hand. -->

# CF Time Units and Non-Conventional Dimension Order

<div class="gc-badges">
<span class="gc-badge">netcdf</span><span class="gc-badge">NetCDF</span><span class="gc-badge">EPSG:4326</span><span class="gc-badge">tiny</span><span class="gc-badge">bundled</span>
</div>

A CF time axis encoded as "hours since 2020-01-01 00:00:00" on a variable whose dimensions run (longitude, latitude, time) -- x before y, time last. Undecoded, the time axis is three small integers meaning nothing; assumed to be (time, latitude, longitude), the array transposes silently.

| Property | Value |
|---|---|
| Case ID | `cf_time_ordering_netcdf` |
| Category | netcdf |
| Format | NetCDF |
| CRS | `EPSG:4326` |
| Location | North Sea / Northern Europe (synthetic) |
| Test tier | unit |
| Size class | tiny |
| Storage class | bundled |
| Redistributable | yes |
| Loader | `xarray` |
| Status | validated |

## Use this case

```python
import pytest


@pytest.mark.geocase_case("cf_time_ordering_netcdf")
def test_cf_time_ordering_netcdf(geocase_case) -> None:
    data = geocase_case.load()
    assert data is not None
```

## Use GeoCase in your tests

Install the complete set of vector, raster, and NetCDF dependencies:

```bash
pip install "geocase[all]"
```

[View GeoCase on PyPI](https://pypi.org/project/geocase/).

## What this case checks

Verify that dimension order is read from the file rather than assumed, and that CF time units are decoded to real timestamps rather than used as raw offsets.

## Risk types covered

- `coordinate_order`
- [`dimension_mismatch`](../risk/dimension-mismatch.md)

## Expected behavior

| Assertion | Expected |
|---|---|
| `expect_loadable` | yes |

## Notes

### Purpose

One file carrying two related CF traps: a time axis stored as offsets from an
epoch, on a variable whose dimensions are **not** in the order most code
assumes.

`t2m` has dims `(longitude, latitude, time)` — x before y, time last. `time` is
`[0, 24, 48]` with `units = "hours since 2020-01-01 00:00:00"` and
`calendar = "gregorian"`.

### Why the two are combined in one fixture

Deliberately, and it is worth stating because the alternative looks tidier.

A `time` dimension has to go **somewhere** in the ordering. Any CF-time fixture
is therefore already making a dimension-order statement, whether or not it
declares one. Splitting these into two cases would produce two files that each
carry both properties while each declaring only one — which is precisely the
defect that got `coordinate_order` removed from `latlon_small`: a label with no
bytes behind it.

### What goes wrong

**Dimension order.** Code that assumes `(time, latitude, longitude)` and indexes
positionally — `values[0]` for "the first timestep", or a bare `.T` — gets a
transposed array. With a square grid it produces a plausible map of nothing; here
the shape is `(8, 5, 3)`, so it usually raises instead, which is the better
outcome and the reason the sizes differ.

**Time units.** Undecoded, the axis is `[0, 24, 48]` — three small integers that
mean nothing without the `units` attribute. Treated as timestamps, hours, or
indices, they all "work" and all give different answers:

```python
xarray.open_dataset(path, decode_times=False)["time"]  # [0, 24, 48]
xarray.open_dataset(path)["time"]                      # 2020-01-01, -02, -03
```

The values are one day apart, not one hour, which is what makes the mistake
visible if anyone checks.

### Typical checks

- `list(ds["t2m"].dims) == ["longitude", "latitude", "time"]`.
- Decoded times land on consecutive days in January 2020.
- `decode_times=False` returns the raw offsets and the `units` string.

### Related

`latlon_small` is the conventional counterpart: `(latitude, longitude)`,
no time axis. It used to declare `coordinate_order` and `dimension_mismatch`
without exercising either; those risk types now live here, on bytes that
demonstrate them.

## Required capabilities

- `load`
- `coordinate-inspection`

## Files

- Primary: `cf_time_ordering.nc`
- Notes: `notes.md`

## Source and license

- Source: geocase-curated
- License: MIT

## Tags

`cf-conventions` `dimension-order` `netcdf` `time`

## Related cases

- [Lat/Lon Small NetCDF](latlon_small.md) -- `latlon_small`
- [Packed int16 NDVI (NetCDF)](ndvi_packed_netcdf.md) -- `ndvi_packed_netcdf`
- [Polygon Z (GeoPackage)](polygon_z_gpkg.md) -- `polygon_z_gpkg`
- [Polygon Z (WKB)](polygon_z_wkb.md) -- `polygon_z_wkb`

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Dataset",
  "name": "CF Time Units and Non-Conventional Dimension Order",
  "description": "A CF time axis encoded as \"hours since 2020-01-01 00:00:00\" on a variable whose dimensions run (longitude, latitude, time) -- x before y, time last. Undecoded, the time axis is three small integers meaning nothing; assumed to be (time, latitude, longitude), the array transposes silently.",
  "identifier": "cf_time_ordering_netcdf",
  "url": "https://farzinashouri.github.io/geocase/_generated/catalog/cases/cf_time_ordering_netcdf/",
  "isPartOf": {
    "@type": "DataCatalog",
    "name": "GeoCase",
    "url": "https://farzinashouri.github.io/geocase"
  },
  "keywords": [
    "cf-conventions",
    "coordinate_order",
    "dimension-order",
    "dimension_mismatch",
    "netcdf",
    "time"
  ],
  "license": "MIT",
  "creator": {
    "@type": "Organization",
    "name": "geocase-curated"
  },
  "distribution": [
    {
      "@type": "DataDownload",
      "encodingFormat": "NetCDF",
      "name": "cf_time_ordering.nc"
    }
  ],
  "spatialCoverage": {
    "@type": "Place",
    "additionalProperty": {
      "@type": "PropertyValue",
      "name": "coordinateReferenceSystem",
      "value": "EPSG:4326"
    },
    "name": "North Sea / Northern Europe (synthetic)"
  }
}
</script>
